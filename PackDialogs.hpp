struct DialogChunk {
	size_t dialog_start;
	size_t name_length;
    size_t text_length;
	size_t choice_length;
	size_t character_length;
	size_t sprite_length;
	size_t background_length;
	size_t background_music_length;
	size_t sound_length;
};